{
  "name": "tech-assignment",
  "version": "1.0.0",
  "description": "# Problem Write a program that solves the most suitable (with highest non-zero speed) network station for a device at a given point (x, y). This problem can be solved in 2-dimensional space. Network stations have reach and speed that depends on the distance to the station. A network station’s speed can be calculated as follows:",
  "main": "network_speed.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app/app.js"
  },
  "author": "",
  "license": "ISC",
  
  "dependencies": {
    "express": "^4.17.2"
  }

}
